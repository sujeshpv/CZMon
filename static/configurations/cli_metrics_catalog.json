{
  "cluster_version": {
    "description": "List of commands to be executed on the cluster",
    "command": ["df -h"],
    "endpoint_type": "PC"
  },

  "cassandra_node_consistency": {
    "description": "Compares svmips with nodetool ring and records per-SVM usage",
    "command": ["nodetool -h 0 ring"],
    "endpoint_type": ["PE"]
  }
}

