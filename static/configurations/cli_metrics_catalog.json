{
  "cluster_version": {
    "description": "List of commands to be executed on the cluster",
    "command": ["df -h"],
    "endpoint_type": "PC"
  },

  "cassandra_node_consistency": {
    "description": "Runs svmips and nodetool ring; Stats UI flags node-count mismatch",
    "command": ["svmips", "nodetool -h 0 ring"],
    "endpoint_type": ["PE"]
  }
}

